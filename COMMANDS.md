
# USEFULL commands
----------------------------------

## GIT
- `git init`,
- `git config --global --add safe.directory "path"` - localization on file servers,
- `git branch -m "main"`
- `curl -sL https://www.toptal.com/developers/gitignore/api/python,pycharm > .gitignore`
- `gh repo create https://github.com/marcinszlaz/img-watermark.git --public`
- `git remote add origin https://github.com/marcinszlaz/img-watermark`
- 
### pip
- `pip install -r requirements.txt`
